# Gmail 
set from = "hugo.forestier123@gmail.com"
set realname = "Hugo Forestier"
set use_from = yes

set imap_user = "hugo.forestier123@gmail.com"
set imap_pass = "`gpg --quiet --for-your-eyes-only --no-tty -d ~/.config/mutt/accounts/gmail_pass.gpg`"
set folder = "imaps://imap.gmail.com/"
set spoolfile = "+INBOX"
set record = "+[Gmail]/Sent Mail"
set postponed = "+[Gmail]/Drafts"

set smtp_url = "smtp://hugo.forestier123@gmail.com@smtp.gmail.com:587/"
set smtp_pass = "`gpg --quiet --for-your-eyes-only --no-tty -d ~/.config/mutt/accounts/gmail_pass.gpg`"
set ssl_starttls = yes
set ssl_force_tls = yes

mailboxes = "+INBOX" "+[Gmail]/Sent Mail" "+[Gmail]/Drafts" "+[Gmail]/Trash"

set header_cache = "~/.config/mutt/cache/gmail/headers"
set message_cachedir = "~/.config/mutt/cache/gmail/bodies"
set certificate_file = "~/.config/mutt/certificates"

set move = no
set imap_keepalive = 900
